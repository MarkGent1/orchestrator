┌──────────────────────────────────────────────────────────────┐
│                        WORK ITEM (ADO)                        │
└───────────────┬──────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────────┐
│                 MODULE 1 — PLANNING (Claude Sonnet)           │
│  WorkItemPlanner                                              │
│  - Fetch Work Item                                            │
│  - Generate plan (tasks)                                      │
│  - Validate acceptance criteria                               │
└───────────────┬──────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────────┐
│           MODULE 4 — SDLC ENGINE (Multi‑Agent Core)           │
│                                                               │
│  Task Decomposition (GPT‑5.4‑mini)                            │
│  - Break tasks into subtasks                                  │
│                                                               │
│  Subtask Execution (Claude Haiku)                             │
│  - Build repo‑aware prompt                                    │
│  - Generate file edits                                         │
│  - Multi‑pass refinement                                       │
│  - Commit per subtask                                          │
│                                                               │
│  Task Memory                                                   │
│  - Track changed files, fixes, context                        │
└───────────────┬──────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────────┐
│               MODULE 3 — VALIDATION (Claude Haiku)            │
│  BuildTestValidator                                           │
│  - Detect repo type                                           │
│  - Backend: build + test                                      │
│  - Frontend: build + test + lint + format                     │
│                                                               │
│  Auto‑Fix Loop (FixLoop — Claude Haiku)                       │
│  - Generate minimal fixes                                     │
│  - Validate with Clean Architecture rules                     │
│  - Apply safely in temp workspace                             │
│  - Re‑run failing phase                                       │
│  - Only proceed if clean                                      │
└───────────────┬──────────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────────┐
│                     MODULE 2 — PR CREATION                    │
│  GitWorkflow                                                  │
│  - Create feature branch                                      │
│  - Push branch                                                │
│  - Open PR                                                    │
│                                                               │
│  PR Enhancer                                                  │
│  - Add plan summary                                           │
│  - Add task memory                                            │
│  - Add build/test logs                                        │
│                                                               │
│  AdoMcpClient                                                 │
│  - Link PR to Work Item                                       │
└──────────────────────────────────────────────────────────────┘
