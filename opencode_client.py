from typing import Any, List, Dict

async def call_opencode(prompt: str) -> List[Dict[str, Any]]:
    """
    Stub: wire this to OpenCode Desktop / API.
    For now, you can:
    - print the prompt
    - paste it into OpenCode manually
    - paste the JSON back into a temp file and load it
    """
    raise NotImplementedError("Wire this to OpenCode Desktop or API.")
